<Window x:Class="CodexTaskMonitor.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:automation="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        Width="330" Height="{Binding PanelHeight}" MaxHeight="458"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True" ResizeMode="NoResize">
  <Border CornerRadius="14" Background="#EE1E242F">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="48" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="Header_MouseLeftButtonDown" automation:AutomationProperties.Name="Codex 任务面板标题栏">
        <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Margin="14,0" VerticalAlignment="Center">
          <TextBlock Text="Codex 任务" FontSize="13" FontWeight="SemiBold" Foreground="White" />
          <TextBlock Text="{Binding Items.Count}" Margin="8,0,0,0" FontSize="11" Foreground="#94A3B8" />
        </StackPanel>
        <Button Grid.Column="1" Content="•••" Width="32" Height="28" Margin="0,0,10,0" Click="More_Click"
                ToolTip="更多选项" automation:AutomationProperties.Name="更多选项">
          <Button.ContextMenu>
            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
              <MenuItem Header="重新读取" Command="{Binding RefreshCommand}" />
              <MenuItem Header="登录时启动" IsCheckable="True" IsChecked="{Binding IsStartupEnabled, Mode=OneWay}" Command="{Binding ToggleStartupCommand}" />
              <Separator />
              <MenuItem Header="退出" Command="{Binding QuitCommand}" />
            </ContextMenu>
          </Button.ContextMenu>
        </Button>
      </Grid>
      <ListBox x:Name="TaskList" Grid.Row="1" ItemsSource="{Binding Items}" MaxHeight="378"
               Background="Transparent" BorderThickness="0"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               ScrollViewer.VerticalScrollBarVisibility="Auto"
               automation:AutomationProperties.Name="Codex 任务列表">
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" /><Setter Property="Margin" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="Focusable" Value="False" />
          </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Border Height="63" BorderBrush="#14FFFFFF" BorderThickness="0,1,0,0">
              <Grid Height="62" Margin="14,0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="18" />
                  <ColumnDefinition Width="*" MinWidth="0" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Ellipse Width="8" Height="8" Fill="{Binding DotColor}" />
                <Button Grid.Column="1" Tag="{Binding}" Click="Open_Click" Background="Transparent" BorderThickness="0"
                        MinWidth="0" ClipToBounds="True" HorizontalContentAlignment="Stretch"
                        ToolTip="打开此 Codex 任务" automation:AutomationProperties.Name="打开任务">
                  <StackPanel>
                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="13"
                               TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                    <TextBlock Foreground="#94A3B8" FontSize="10.5"><Run Text="{Binding StateText, Mode=OneWay}" /><Run Text=" · " /><Run Text="{Binding ProjectName, Mode=OneWay}" /></TextBlock>
                  </StackPanel>
                </Button>
                <Button Grid.Column="2" Tag="{Binding}" Click="Dismiss_Click" Content="已处理" FontSize="10.5" Padding="7,4"
                        ToolTip="将此任务标记为已处理" automation:AutomationProperties.Name="标记为已处理">
                  <Button.Style>
                    <Style TargetType="Button">
                      <Setter Property="Visibility" Value="Visible" />
                      <Style.Triggers><DataTrigger Binding="{Binding CanDismiss}" Value="False"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers>
                    </Style>
                  </Button.Style>
                </Button>
              </Grid>
            </Border>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
      <Border Grid.Row="2" Height="32" Background="#0EF59E0B" automation:AutomationProperties.LiveSetting="Polite">
        <Border.Style>
          <Style TargetType="Border">
            <Setter Property="Visibility" Value="Visible" />
            <Style.Triggers><DataTrigger Binding="{Binding HasError}" Value="False"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers>
          </Style>
        </Border.Style>
        <TextBlock Text="{Binding ErrorMessage}" Margin="14,0" VerticalAlignment="Center" Foreground="#FBBF24" FontSize="10.5" TextTrimming="CharacterEllipsis" />
      </Border>
      <Border Grid.RowSpan="3" CornerRadius="14" BorderBrush="#26FFFFFF" BorderThickness="1" IsHitTestVisible="False" />
    </Grid>
  </Border>
</Window>
